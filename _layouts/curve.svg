<svg xmlns="http://www.w3.org/2000/svg" role="img"  width="460" height="100">
  <path d="M 460,100 H 0 V 0 C
    {% for d in site.data.results %}
      {{ forloop.index }}0 {{ 100 | minus:d[1].score }}
    {% endfor %}"
    fill="#CCC" />

  <line stroke-dasharray="5, 5" x1="{{ page.rank }}0" y1="{{ 100 | minus:page.score }}" x2="{{ page.rank }}0" y2="100" />

<style><![CDATA[
line{
    stroke: black;
    stroke-width: 2;
}
]]></style>
</svg>
